'use client';

import UploadPage from '@/app/(app)/upload/page';

// Re-export: /contratos/upload maps to the existing upload page
export default function ContratosUploadPage() {
    return <UploadPage />;
}
